<match **.nifi.platform**>
  @type rewrite_tag_filter
  <rule>
    key message
    pattern /^ConsumeMQTT\[.*\] Connection to tcp:\/\/[^\s]+ lost .*$/
    tag signal.MqttConnectionError
   </rule>
  <rule>
    key message
    pattern /^.+$/
    tag noise
  </rule>
</match>
